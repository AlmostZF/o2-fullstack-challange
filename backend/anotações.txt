Entidades

Produtos:

id: UUID
name: String
descricao: String
quantidade: int
valorUnitario: int
data_criacao: dateTime
data_modificacao: dateTime
estoque_minimo: int - usado para alertar stoque
deleted: boolean / status: enum 'active', 'deleted',

Movimentacao

id: PK
product_id: FK do Produtos.id
tipo: enum 'entrada' e 'saida'
quantidade: int
data_movimentacao: timestemp
observacao: string

Relacionamento

Produtos (1) ←→ (N) Movimentacao

Usuário ? se  sobrar tempo

id: UUID
nome: string
email: string
senha_hash: string
papel (admin, operador, etc.)